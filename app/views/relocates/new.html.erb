<h4>Перемещение товара</h4>
<br />
<%= form_with model: @relocate, local: true do |form| %>
  <div class="field">
    <p>
      <%= form.text_field :number, placeholder: "Введите номер товара" %>
    </p>
  </div>

  <div class="field">
    <p>
      <%= form.text_field :name, placeholder: "Введите адрес из которого перемещается товар" %>
    </p>
  </div>

  <div class="field">
    <p>
      <%= form.text_field :relocated_to, placeholder: "Введите адрес куда размещается товара" %>
    </p>
  </div>

  <div class="field">
    <p>
      <%= form.number_field :quantity, min: 1, placeholder: "Введите количество" %>
    </p>
  </div>
  
  <div class="actions">
    <p>
      <%= form.submit "Подтвердить" %>
    </p>
  </div>
<% end %>
